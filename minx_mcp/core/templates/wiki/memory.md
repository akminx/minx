---
type: minx-memory
scope: ${scope}
memory_key: ${memory_key}
memory_type: ${memory_type}
subject: ${subject}
memory_id: ${memory_id}
sync_base_updated_at: "${sync_base_updated_at}"
payload_json: '${payload_json}'
---

# ${name}

## Summary

<!-- Brief memory summary maintained by Hermes or the user -->

## Human Editable

<!-- Add ordinary notes here. Core only syncs structured frontmatter. -->

## System Metadata

Frontmatter is Minx sync metadata. Normal edits should happen in Human Editable.
