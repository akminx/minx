# Project Handoff

Status as of 2026-04-18: Slices 1 through 4 are implemented, consolidation/code-quality/observability hardening is complete, and **Slice 6 phases 6a-6c are implemented in the current working tree**. Slice 6a/6b provide durable memory schema/service/MCP tools, first detectors, and snapshot archives. Slice 6c adds migration `018_vault_index.sql`, the `VaultScanner`, `vault_index`, `memory_events.vault_synced`, the diagnostic `vault_scan` MCP tool, structured memory-ingest failure reports, stricter event/drop logging, and the folded hardening items from the 6c plan. The current tree also contains early 6d/6e primitives: snapshot `MemoryContext` DTOs, wiki template package data/resources, and `vault_replace_section`. Two subagent reviews were run after implementation; their blockers (walk-failure orphan wipe, loose memory identity, ungated orphan events, and unsurfaced memory-ingest failures) were fixed and regression-tested. A third senior-engineer review pass surfaced 10 further correctness/safety issues across 6a/6b/6c (rowcount-guarded VaultScanner UPDATEs, `vault_scan` audit log, `memory_key` whitespace asymmetry, migration 018 crash-safety, scan write-lock starvation, `prune_expired_memories` auto-commit, stale `vault_index.memory_id` on terminal memories, dry_run/migration/terminal-rescan test gaps, missing `__init__.py` for wiki templates, unconditional prune in `_memory_list`); all 10 landed with regression tests, plus an 11th item (`sync_base_updated_at` reserved-key) flagged by a greenlight review of the fix diffs. Slice 6d-6f remain the next implementation focus: finish/harden snapshot memory context as needed, complete deterministic wiki write surfaces, and implement safe vault reconciliation/conflict policy. Slice 8 (Proactive Autonomy) is fully designed and queued after Slice 6.

Hermes cutover snapshot (2026-04-14):

- Minx MCP ports: finance `8000`, core `8001`, meals `8002`, training `8003`
- Legacy `financehub` and `souschef` are disabled (kept in config for rollback)
- Hermes Minx skills and cron definitions have been rewritten/pinned to Minx MCP tool paths

## Repo And Branch

- Remote: `github.com/akminx/minx`
- Branch: `main`
- Stack: Python 3.12, FastMCP, SQLite, Pydantic, pytest, mypy
- Current health: test suite passing (`uv run pytest -q` -> 867 passed); production mypy clean (`uv run mypy minx_mcp` -> 0 errors); Ruff clean (`uv run ruff check .`); verify via commands below

## What Is Minx

Minx is a personal Life OS built as MCP servers.

- Domain MCPs own user facts and domain operations (Finance, Meals, Training).
- Minx Core owns deterministic interpretation (read models, detectors, insight history, snapshots, memory).
- Hermes (or any harness) owns narrative, coaching, conversation flow, scheduling, and wiki maintenance.

Governing rule: **data and deterministic logic live in MCP; conversational policy, scheduling, and LLM prose live in harness.**

## Architecture Decisions

Key architectural decisions that govern future work:

1. **Core/Harness split (Slice 2.5)**: Core provides structured data and tools. Harnesses own orchestration, scheduling, notifications, and LLM-generated prose. This applies to both memory (Slice 6) and autonomy (Slice 8).
2. **Memory tiers (Slice 6)**: Three-tier memory: SQLite for structured facts (Tier 1), SQLite for episodic archives (Tier 2), Obsidian vault as a living wiki (Tier 3, LLM Wiki pattern). Core reads the vault and stores facts. Harness writes wiki pages using LLM via Core tools.
3. **Autonomy split (Slice 8)**: Core provides an audit trail (`playbook_runs`), logging/history MCP tools, and a `playbook://registry` resource. Hermes owns scheduling (uses existing cron), playbook execution scripts, confirmation conversations, and wiki maintenance. No scheduling library in Core.
4. **LLM Wiki pattern (Slices 6+8)**: Inspired by Karpathy — Obsidian is the IDE, the LLM is the programmer, the wiki is the codebase. Minx incrementally builds and maintains structured wiki pages in Obsidian rather than re-deriving knowledge on every query.

## Implemented Slices


| Slice                                                            | Status                   | Outcome                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------------------------------------------------- | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1: Event Pipeline + Daily Review                                 | Implemented              | Event contracts, finance events/read APIs, core read models and baseline detectors                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2: Goals + Deeper Detection                                      | Implemented              | Goals CRUD/trajectory and deeper drift-style detection                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2.1: Conversational Goals + Trust                                | Implemented              | Structured and natural goal capture path with trust/sensitivity boundaries                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2.5: MCP Surface Refactor (+ cleanup)                            | Implemented              | Core MCP tool cleanup (`get_daily_snapshot`, `get_insight_history`, `get_goal_trajectory`, `persist_note`, `goal_parse`, `finance_query`) and cleanup fixes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 3: Meals MCP                                                     | Implemented              | Meals logging/planning, nutrition read model integration, meal/nutrition events and detectors                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 4: Training MCP Skeleton + Integration                           | Implemented              | Training domain server/service/schema/read API/events/progression, core snapshot/detector integration, harness-start scripts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 6a: Memory schema + MemoryService + CRUD tools + first detectors | Implemented (2026-04-17) | `memories` + `memory_events` tables, lifecycle (create/confirm/reject/expire + auto-promote at 0.80), `MemoryService.ingest_proposals` dedupe/merge, three detectors (recurring merchant / category preference / schedule), seven MCP tools                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 6b: Snapshot archives                                            | Implemented (2026-04-17) | `snapshot_archives` table with content-hash dedupe, `build_daily_snapshot` auto-persist, `list_snapshot_archives` and `get_snapshot_archive` MCP tools                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 6c: Vault scanner + vault_index                                  | Implemented (2026-04-18) | Migration `018_vault_index.sql` adds `vault_index` and re-adds `vault_synced` to the memory event CHECK; `VaultScanner` walks `Minx/` through `VaultReader`, indexes frontmatter metadata/content hashes, syncs valid `minx-memory` notes through the required `memory_key` identity contract, creates/updates/confirms active memories with `actor='vault_sync'`, skips terminal memories, gates orphan events to active `source='vault_sync'` rows, and skips orphan cleanup after incomplete vault walks. Core exposes diagnostic `vault_scan(dry_run=False)`; no automatic snapshot scan is wired yet. Fold-in hardening also landed: structured `IngestProposalsReport`, expired active rows filtered even with `status=None`, strict `emit_event`, finance rule priority/warnings, preference/job/time/launcher/document-text robustness, wiki template package data, and subagent review fixes. |
| 6 hardening                                                      | Implemented (2026-04-17) | Partial unique index `UNIQUE(memory_type, scope, subject) WHERE status IN ('candidate','active')`, `expire_memory` restricted to `active`-only (rejection is truly terminal), `create_memory` maps the partial-unique violation to `CONFLICT`, `scope` filter on list/pending tools, `VaultReader` handles UTF-8 BOM, `VaultWriter` raises `InvalidInputError`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Slice 6c post-completion review fixes                            | Implemented (2026-04-18) | Ten correctness/safety fixes from a senior-engineer review of the 6c implementation. **(1)** `VaultScanner._sync_memory_note` candidate and active UPDATE paths now check `cur.rowcount == 0` and return `_MemorySyncResult(memory_id=None, clear_memory_id=False)` on concurrent status change — no orphan events emitted, warning logged. **(2)** `_vault_scan` in `core/server.py` now calls `log_sensitive_access(conn, "vault_scan", ...)` before the scanner runs, matching finance's audit pattern. **(3)** `_parse_memory_identity` now strips each segment of `memory_key.split(".", 2)` before comparisons and before returning `subject`, eliminating trailing-space asymmetry. **(4)** `018_vault_index.sql` adds `DROP TABLE IF EXISTS memory_events_018_new` before the `CREATE TABLE` to guard against crash-at-mid-migration re-runs; comment documents checksum-change recovery step for DBs that already applied the old hash. **(5)** `VaultScanner.scan()` restructured into two phases: phase 1 (`_walk_vault`) is a pure file walk with no DB lock held; phase 2 (`_index_documents`) opens `BEGIN IMMEDIATE` over the collected list — eliminates writer starvation during file I/O. **(6)** `MemoryService.prune_expired_memories` no longer calls `self.conn.commit()` (callers are responsible); both callers in `server.py` (`_memory_list`, `_get_pending_memory_candidates`) now call `conn.commit()` explicitly after prune. **(7)** `_sync_memory_note` returns a `_MemorySyncResult` dataclass with `memory_id` and `clear_memory_id` flag; terminal-memory skip sets `clear_memory_id=True` so the outer `vault_index` UPDATE explicitly sets `memory_id = NULL` rather than using `COALESCE`. **(8)** Three spec §9 tests added: (a) `vault_scan(dry_run=True)` does not persist `vault_index` or `memories`, (b) re-scanning an unchanged note whose memory was later expired warns and clears `vault_index.memory_id`, (c) migration 018 applied to a pre-6c DB (017-state with seeded `memory_events`) preserves rows and accepts `vault_synced` events. **(9)** Added empty `__init__.py` in `minx_mcp/core/templates/` and `minx_mcp/core/templates/wiki/` so setuptools treats them as packages; added `"minx_mcp.core.templates" = []` to `pyproject.toml` `[tool.setuptools.package-data]`; import test added. **(10)** `_memory_list` now calls `prune_expired_memories` unconditionally (not just for `status="active"`), matching `_get_pending_memory_candidates`. **(11)** `sync_base_updated_at` added to `_RESERVED_MEMORY_KEYS` in `vault_scanner.py` so the field — used by 6f as version metadata for conflict resolution — does not leak into stored payload JSON when present in note frontmatter; new `test_vault_scanner_excludes_sync_base_updated_at_from_payload` regression test. Surfaced by a follow-up greenlight review subagent against the diffs of fixes 1–10. 16 new regression tests; 851 → 867 total. |
| Packaging + scaffolds hygiene                                    | Implemented (2026-04-17) | Removed redundant `schema/migrations/` repo-root mirror (single source of truth: `minx_mcp/schema/migrations/`); moved finance report templates into `minx_mcp/finance/templates/` and wired them via `[tool.setuptools.package-data]` so wheel installs can render reports; shipped `minx_mcp/meals/templates/recipe-starter.md` as package data + `minx_mcp.meals.templates` loader module (`recipe_starter_template_path`, `read_recipe_starter_template`) + `recipe_template` MCP tool on minx-meals; wheel-packing test `tests/test_db.py::test_built_wheel_includes_packaged_resources` now guards all packaged assets                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| HTTP smoke coverage for memory + recipe scaffold                 | Implemented (2026-04-17) | `tests/test_hermes_http_stack_smoke` now exercises the full Slice 6a memory lifecycle (candidate create at 0.5, auto-promote at 0.9, `get_pending_memory_candidates` with scope filter, `memory_confirm`, `memory_expire`) plus a duplicate-live-triple `CONFLICT` error envelope and a `recipe_template` call — all over a real streamable HTTP transport against four separately spawned MCP server processes; `_call_tool` return type tightened to `dict[str, Any]` which drops 9 pre-existing mypy errors from the baseline (205 → 196) without changing runtime behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Post-6b hardening wave                                           | Implemented (2026-04-17) | Five non-overlapping packets reviewed and landed together: **(A)** `MemoryService` lifecycle mutators (`confirm`, `reject`, `expire`, `update_payload`, ingest merge) now use guarded `UPDATE … WHERE id = ? AND status = ?` + `rowcount` checks — concurrent lifecycle collisions raise typed `ConflictError(data={"memory_id","expected_status"})` instead of silently clobbering a rejection or expired row. **(B)** `build_daily_snapshot` offloads its SQLite body via `asyncio.to_thread`, and `memory_service.ingest_proposals` is wrapped best-effort so memory-layer exceptions don't abort snapshot archive persist (symmetric with detector-insight persistence). **(C)** `MealsService.index_recipe` wrapped in a SQLite `SAVEPOINT` (mid-loop failure no longer leaves partial ingredient rows); `nutrition.day_updated` now actually emits after `meal.logged` with aggregated day totals (best-effort with warning on emission failure, documented asymmetry with `meal.logged`); `goal` threaded into `calculate_nutrition_targets` via `GOAL_CALORIE_MULTIPLIERS` (`cut=0.85`, `maintenance=1.0`, `bulk=1.10`) applied to TDEE before deficit; recipe `nutrition:` frontmatter parsed into `nutrition_summary_json` (inline JSON or indented block); duplicated timezone helpers across meals/training consolidated into `minx_mcp/time_utils.py`. **(D)** Finance dedupe fingerprint now includes normalized merchant when `external_id` is empty (prior algorithm silently collapsed legitimate same-day/same-amount/different-merchant rows); CSV/PDF importers enforce `MAX_FINANCE_IMPORT_ROWS=100_000` + `MAX_FINANCE_IMPORT_FILE_BYTES=50 MiB` caps (streaming-correct on both paths); finance report models switched from float dollars to integer `*_cents` fields with `format_decimal_cents` at render; DCU parser normalizes `posted_at` to ISO `YYYY-MM-DD`. **(E)** `MINX_HTTP_PORT` env validated with clean error; `JSONFormatter` redacts secret-shaped substrings and truncates at 4096 chars; `db.add_column_if_missing` allowlists SQLite type fragments; `VaultReader` / `VaultWriter` reject symlink escapes via post-`resolve()` `is_relative_to(vault_physical)`; `document_text` caps `CalledProcessError` stderr preview at 512 chars (full stderr still logged at WARNING); `memory_expire` MCP tool defaults `actor="system"`. 36 new regression tests landed across the five packets |
| Post-hardening comprehensive audit (Waves 1–3)                   | Implemented (2026-04-17) | A second, whole-repo audit (four parallel `explore` subagents across core/harness, concurrency/async, domain contracts, and type/error-envelope surfaces) produced a new prioritized list of issues missed by the first hardening pass; landed in three verification-fenced waves. **Wave 1** — `VaultWriter.replace_section` / `write_markdown` now hold an `fcntl.flock` advisory lock on a sibling `.{filename}.lock` file (8 s timeout → typed `ConflictError`) so concurrent vault writers no longer interleave read-modify-write; `VaultWriter` reads with `utf-8-sig` (was `utf-8`) to match `VaultReader` on BOM'd notes; detector registry key normalized from `finance.open_loops` to `finance.open_loop` so `list_history` lookups by the persisted `insight_type` actually match; `reject_memory` now stamps `expires_at = now + 30 days`, `list_memories` / `list_pending_candidates` defensively drop any `status='active'` row whose `expires_at` is past, new `prune_expired_memories()` deletes expired rejected rows and is called before memory listing; migration `016_memory_ttl_and_event_check.sql` rebuilds `memory_events` with a narrower `event_type` CHECK (`created`, `promoted`, `confirmed`, `rejected`, `expired`, `payload_updated` only — `vault_synced` will be re-added in 6c); `money.parse_dollars_to_cents` accepts `$` prefix + US comma thousands separators; Discover + Robinhood Gold parsers normalize `posted_at` to ISO `YYYY-MM-DD`; `match_pantry` aggregates duplicate `(normalized_name, unit)` rows (sums quantities, earliest expiration, merged `sources` list) and logs a warning on unit conflicts. **Wave 2** — `BaseService.conn` is now backed by a per-instance `contextvars.ContextVar` instead of `threading.local()`, giving each asyncio Task its own SQLite connection (previously sibling coroutines on the same OS thread could share a connection mid-transaction, which is unsafe in SQLite); legacy `_local` retained as a compatibility shim for subclasses (`MealsService.from_connection`, `FinanceService._uncategorized_id`) that intentionally share a connection. Recipe row lifecycle: migration `017_recipes_vault_synced_at.sql` rebuilds `meals_recipes` with `vault_path TEXT NULL` + `vault_synced_at TEXT NULL` (SQLite cannot drop `NOT NULL` in place), `index_recipe` stamps `vault_synced_at`, new `MealsService.reconcile_vault_recipes()` soft-deletes the `vault_path` / `vault_synced_at` for recipes whose `.md` file disappeared and emits a new `meals.recipe_orphaned` event per orphan; `scan_vault_recipes` calls reconcile first; a new `recipes_reconcile` MCP tool exposes the operation. **Wave 3** — `contracts.LLMError` (code `LLM_ERROR`) added and `core.llm.LLMProviderError` / `LLMResponseError` reparented off it; `interpretation/runner.py` now raises `LLMError` (not `RuntimeError`) when LLM JSON is bad or schema validation fails, so clients can distinguish upstream-model failures from server bugs. `minx_mcp/core/memory_payloads.py` defines per-type `extra="forbid"` pydantic models (`PreferencePayload`, `PatternPayload`, `EntityFactPayload`, `ConstraintPayload`); `MemoryService.create_memory` / `update_payload` validate through them (→ `InvalidInputError`), `ingest_proposals` validates per proposal and skips bad ones with a warning instead of aborting the batch. `_goal_parse` (core) and `_finance_query` (finance) both restructured into read-serialize → close-connection → `await` LLM → reopen-connection → persist phases so no SQLite writer lock is held across LLM network I/O. `run_finance_import` moved `parse_source_file` (up to 30 s for PDFs) above `mark_running` + `SAVEPOINT finance_import` so parsing no longer pins the finance DB's writer lock. `log_meal` savepoint extended to cover the `nutrition.day_updated` emission (previously split across two commits); `_emit_nutrition_day_updated` now raises `RuntimeError` on emission failure to trigger rollback — either both the meal row + `meal.logged` + `nutrition.day_updated` land or none of them do. ~28 new regression tests landed across the three waves (vault lock probes, TTL pruning, BaseService task isolation, recipe reconcile, LLM error envelopes, DB-lock-during-LLM-await probes, `log_meal` atomicity). All waves verified with `mypy minx_mcp` → 0 errors, `ruff check .` clean, `pytest -q` 798 passing. |
| Skeptical post-audit review — critical correctness fixes         | Implemented (2026-04-17) | A further skeptical review pass (four parallel `code-reviewer` subagents over the Wave 1–3 diffs) surfaced five correctness bugs that were not caught by earlier reviews; all five landed behind regression tests. **(1) Money parsing — multi-comma corruption**: `parse_dollars_to_cents` previously ran `raw.replace(",", "")` unconditionally, so `"1,2,3.45"` silently became `123.45` and European-style `"1.234,56"` became `1234.56`. Added a pre-strip `_US_GROUPED_AMOUNT` regex that fullmatch-validates `"-?(\d{1,3}(,\d{3})+(\.\d+)?\|\d+(\.\d+)?\|\.\d+)"` whenever any comma is present, so malformed grouping raises `InvalidInputError("amount uses malformed thousands grouping (expected e.g. '1,234.56')")` before any value-destroying normalization runs. **(2) Memory payload shape expansion**: `validate_memory_payload` used `model_dump(exclude_none=False)`, which expanded an empty `{}` posted to a `PreferencePayload` into `{"entity":None,"value":None,"tags":None,"notes":None}` in the stored row — silently changing the on-disk JSON shape. Switched to `model_dump(exclude_unset=True)` so the DB row now faithfully echoes what the client sent. **(3) Memory ingest merge bypassed validation**: `MemoryService.ingest_proposals` merge branch union'd the new (validated) proposal payload onto the prior (unvalidated) stored payload, so any legacy junk persisted from pre-6a data would compound forward through every subsequent merge. Added `coerce_prior_payload_to_schema(memory_type, prior_payload)` in `memory_payloads.py` that filters to `model.model_fields`, attempts validation, and returns `{}` on unrecoverable legacy data — ingest merge now normalizes the prior side before union. **(4) `scan_vault_recipes` reconcile inside savepoint**: previously `scan_vault_recipes` called the private reconcile helper (which runs inside the outer transaction), then iterated `index_recipe` which uses `SAVEPOINT meals_index_recipe`. In SQLite, `ROLLBACK TO SAVEPOINT` does NOT undo statements issued BEFORE the savepoint — and more critically, a mid-loop raise + method exit would leave the reconcile's `UPDATE meals_recipes` + `meals.recipe_orphaned` events in an uncommitted outer transaction that had no guaranteed commit path. Refactored to call the public `reconcile_vault_recipes()` (self-managed transaction + commit) BEFORE entering the index loop, so reconcile is durable regardless of later `index_recipe` outcomes. **(5) Finance import queued-window race**: two concurrent callers with the same idempotency key could both pass the `status in {running, completed}` gate while a row was still `queued`, both parse the file (30 s PDFs), and `mark_running` would silently overwrite each other. Added `jobs.claim_queued_job(conn, job_id) -> bool` that performs an atomic `UPDATE jobs SET status='running' WHERE id=? AND status='queued'` + `commit()` + `job_events` audit row — the loser returns `False` and the caller re-fetches + returns the current state without parsing. `run_finance_import` now claims BEFORE parsing, and wraps the post-parse SAVEPOINT in an explicit `BEGIN IMMEDIATE` so `RELEASE SAVEPOINT` cannot promote to an implicit commit and break rollback when `mark_completed` raises. 9 new regression tests landed (3 money, 2 memory-shape, 2 memory-merge, 1 scan-reconcile-durability, 3 import-claim-race). All verified with `mypy minx_mcp` → 0 errors, `ruff check .` clean, `pytest -q` 817 passing (up from 798). |
| Post-hardening review follow-ups                                 | Implemented (2026-04-17) | Addressed two blocking issues surfaced by a full code review pass: **(1) Meals MCP goal default drift** — `nutrition_profile_set` MCP tool default was still `"fat_loss"` while service default moved to `"maintenance"`; MCP now defaults to `"maintenance"` and a new `test_nutrition_profile_set_mcp_default_goal_matches_service` regression test asserts omit-goal parity between MCP and service callers. **(2) Finance dedupe post-upgrade safety** — the new merchant-aware fingerprint does not match rows stored under the old algorithm, so re-importing the same file post-upgrade would produce duplicates; shipped `rebuild_dedupe_fingerprints(conn)` in `minx_mcp/finance/dedupe.py` + a one-shot CLI at `scripts/rebuild_finance_dedupe.py` + a regression test. **Operators must run `python -m scripts.rebuild_finance_dedupe [DB_PATH]` once after pulling this wave**; the script is idempotent and savepoint-wrapped. Also split `test_reject_race_does_not_overwrite_active` into the correct pair — a sequential pre-check test and a real proxy-based race test that forces svc_b's pre-read to fire between svc_a's concurrent confirm — and added a multi-meal same-day aggregation test for `nutrition.day_updated`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Pre-6c correctness fixes                                         | Implemented (2026-04-17) | A fourth full-repo audit (four `explore` subagents) produced a 14-item prioritized issue list; the four critical/high-severity items landed immediately and the remaining ten are folded into the Slice 6c plan as in-scope mini-tasks. **(1) `LLM_ERROR` contract leak in goal capture** — `minx_mcp/core/goal_capture_llm.py::_capture_with_llm` previously caught bare `Exception` around `_run_goal_capture_interpretation`, which silently converted documented `LLMError` (schema validation / structured decode failures) into a regex fallback. Clients could no longer distinguish "LLM produced garbage" from "regex couldn't match". The handler now re-raises `LLMError` unchanged (routed by `wrap_async_tool_call` → `LLM_ERROR` error envelope) and only the residual generic-`Exception` branch falls back to regex + WARNING log. A new `test_contract_llm_error_propagates_instead_of_regex_fallback` regression test asserts the propagation. **(2) Recipe parser BOM + CRLF fragility** — `minx_mcp/meals/recipes.py::parse_recipe_note` previously read with `encoding="utf-8"` (no BOM stripping) and split frontmatter with hard-coded `\n`, so any recipe authored on Windows (CRLF) or saved with a UTF-8 BOM failed frontmatter detection and landed as an empty `frontmatter` / whole-file `body`. Now reads raw bytes (stable `content_hash`), decodes with `utf-8-sig`, and the `_split_frontmatter` helper uses `content.splitlines()` so `\r\n`, `\n`, and `\r` fences all match. Two new regression tests (`test_parse_recipe_note_handles_crlf_line_endings`, `test_parse_recipe_note_handles_utf8_bom`) lock both paths in, matching the behavior `VaultReader.iter_documents` already had. **(3) Finance sensitive-query count semantics drift** — `minx_mcp/finance/analytics.py::sensitive_query_count` counted *all* transactions (credits + debits) while its paired `sensitive_query_total_cents` and the canonical `FinanceReadAPI.get_filtered_transaction_count` both filtered to spending (`amount_cents < 0`). Any caller comparing "count of transactions" to "sum of spending" over the same filter set was double-counting refunds. Added `t.amount_cents < 0` to the count's WHERE clause and updated the audit-log intent string to `count_spending_transactions`; `goal_progress.py` verified to route through `FinanceReadAPI`, not `sensitive_query_count`, so no latent metric regression. New `test_sensitive_query_count_is_spending_only_and_matches_total` pins the two sides in parity. **(4) Meals vault-path traversal in reconcile** — `minx_mcp/meals/service.py::_reconcile_vault_recipes_inner` trusted `vault_path` from the DB verbatim against `vault_root / relative`, so a row whose `vault_path` had been poisoned with `../outside.md` (or any path that `resolve()` promoted outside the vault) would escape the root check and be silently treated as "synced" if the attacker-controlled target existed on disk. The reconciler now `resolve()`s each candidate, verifies `is_relative_to(vault_root)`, and classifies failures into three orphan reason codes (`vault_path_escapes_root`, `vault_path_unresolvable`, `vault_file_missing`); the existing missing-file event remains backward-compatible. New `test_reconcile_vault_recipes_orphans_paths_that_escape_vault_root` covers the escape case end-to-end. All four fixes + their five new regression tests verified with `pytest -q` (822 passing, up from 817), `mypy minx_mcp` → 0 errors, `ruff check .` clean; independent `code-reviewer` subagent sign-off against the diffs + the 6c plan recorded in `docs/superpowers/plans/2026-04-17-slice6c-vault-scanner.md`. |


## Priority Roadmap (Post-Consolidation)


| Item                                                                           | Status                        | Spec/Plan                                                                                                    |
| ------------------------------------------------------------------------------ | ----------------------------- | ------------------------------------------------------------------------------------------------------------ |
| Consolidation + code quality                                                   | Completed (2026-04-17)        | [cleanup.md](docs/superpowers/plans/cleanup.md), [consolidation.md](docs/superpowers/plans/consolidation.md) |
| Observability + CI hardening                                                   | Completed (2026-04-17)        | [consolidation-and-refactor.md](docs/superpowers/plans/2026-04-15-consolidation-and-refactor.md)             |
| Slice 6a–6b: Durable Memory foundations                                        | Shipped 2026-04-17            | [slice6-durable-memory.md](docs/superpowers/specs/2026-04-15-slice6-durable-memory.md)                       |
| Slice 6c: Vault scanner + `vault_index` + scanner hardening                    | Implemented 2026-04-18        | [2026-04-17-slice6c-vault-scanner.md](docs/superpowers/plans/2026-04-17-slice6c-vault-scanner.md), [6c-6f revised spec](docs/superpowers/specs/2026-04-18-slice6cdef-vault-scanner-memory-context-wiki-sync.md) |
| Slice 6d–6f: MemoryContext hardening, wiki primitives, vault reconciliation    | Next                          | [6c-6f revised spec](docs/superpowers/specs/2026-04-18-slice6cdef-vault-scanner-memory-context-wiki-sync.md)  |
| Slice 8: Proactive Autonomy                                                    | Next after Slice 6 (designed) | [slice8-proactive-autonomy.md](docs/superpowers/specs/2026-04-15-slice8-proactive-autonomy.md)               |
| Slice 7: Journal MCP                                                           | Deferred                      | Standard CRUD, same pattern as Meals/Training, build when wanted                                             |
| Slice 5: Harness Adaptation                                                    | Deferred                      | One harness (Hermes) today; add harness-specific behavior directly to Core/Hermes as needed                  |
| Slice 9: Dashboard                                                             | Deferred                      | Independent technology layer, no MCP dependencies                                                            |


## Current MCP Surface (High Level)

- Core tools: `get_daily_snapshot`, `get_insight_history`, `get_goal_trajectory`, `persist_note`, `vault_replace_section`, `vault_scan`
- Goal tools: `goal_parse`, `goal_create`, `goal_list`, `goal_get`, `goal_update`, `goal_archive`
- Finance tools: `finance_query` (+ existing finance domain operations)
- Meals domain: meal logging/planning + nutrition summary flows + `recipe_template` (returns the packaged `recipe-starter.md` scaffold for users/harness to author indexer-compatible recipe notes)
- Training domain: exercise/program/session/progress flows
- **Memory tools (Slice 6a/6b/6c)**: `memory_list(status?, memory_type?, scope?, limit?)`, `memory_get`, `memory_create(memory_type, scope, subject, confidence, payload, source, reason?)`, `memory_confirm`, `memory_reject` (candidate-only), `memory_expire` (active-only), `get_pending_memory_candidates(scope?, limit?)`, `list_snapshot_archives`, `get_snapshot_archive`, `vault_scan(dry_run?)`
- **Core wiki resources (early 6e primitives)**: `wiki-templates://list`, `wiki-templates://{name}` for `entity`, `pattern`, `review`, and `goal`

## MCP Tool Naming Convention

Tool names are not uniform across domains today: Meals exposes short, unprefixed names (legacy surface), while Training and Finance use domain-prefixed tool names; that inconsistency is intentional for the current Hermes harness and smoke configs, which pin concrete tool identifiers per server process, so renaming tools (either adding prefixes to Meals or dropping them elsewhere) would break those integrations until Hermes and the in-repo HTTP smoke tests are updated in lockstep—**renaming is explicitly deferred** until coordinated with Hermes, and reviewers who flag the mismatch should be pointed to this section rather than opening drive-by rename PRs.

### Planned MCP Surface Additions

Slice 6d–6f will add or finish:

- `MemoryContext` follow-through on `DailySnapshot` (6d) — DTO surface exists in the current tree; next pass should decide if any additional harness-facing fields are needed
- Wiki write hardening (6e) — `vault_replace_section` + packaged `wiki-templates://...` resources exist; next pass should lock resource tests and any Hermes-side usage
- Vault reconciliation (6f) — implement the revised `memory_id` / `memory_key` / `sync_base_updated_at` conflict policy from the 2026-04-18 revised spec

Meals Phase 3 (deferred) will add:

- A `minx_mcp/meals/templates/shopping-list.md` `string.Template` scaffold (same packaging pattern as the finance templates and the Phase 6e wiki scaffolds) for deterministic SQL-backed shopping-list renders — see Slice 3 spec §"Phase 3: Shopping List Generation"

Slice 8 will add:

- `log_playbook_run`, `playbook_history`
- `playbook://registry` MCP resource

## Hermes Harness Readiness

- Startup helper is available at `scripts/start_hermes_stack.sh` for bringing up Finance/Core/Meals/Training MCP services.
- Slice 4 smoke script exists at `scripts/hermes_slice4_smoke.py` for harness-facing integration checks.
- A real Hermes-style streamable HTTP MCP smoke test exists at `tests/test_hermes_http_smoke.py`; it starts all four servers on temporary ports and verifies cross-domain tool calls over `/mcp`. As of 2026-04-17 the smoke also drives the full Slice 6a durable-memory lifecycle end-to-end over HTTP (candidate creation at confidence 0.5, high-confidence auto-promote at 0.9, `get_pending_memory_candidates` with scope filter, `memory_confirm`, `memory_expire`, plus a duplicate-live-triple `CONFLICT` structured error envelope) and calls `recipe_template` on minx-meals to prove the packaged recipe scaffold renders verbatim through a real streamable HTTP transport.
- Slice 8 will add playbook runner scripts and wiki maintenance to Hermes cron.

## Canonical Specs And Inputs

- Architecture: [docs/superpowers/specs/2026-04-06-minx-life-os-architecture-design.md](docs/superpowers/specs/2026-04-06-minx-life-os-architecture-design.md)
- Slice roadmap: [docs/superpowers/specs/2026-04-06-minx-roadmap-slices.md](docs/superpowers/specs/2026-04-06-minx-roadmap-slices.md)
- Slice 3 handoff: [docs/superpowers/plans/2026-04-13-slice3-nutrition-meals-handoff.md](docs/superpowers/plans/2026-04-13-slice3-nutrition-meals-handoff.md)
- Slice 3 phase 3 handoff: [docs/superpowers/plans/2026-04-13-slice3-phase3-shopping-list-handoff.md](docs/superpowers/plans/2026-04-13-slice3-phase3-shopping-list-handoff.md)
- Slice 4 skeleton spec: [docs/superpowers/specs/2026-04-13-slice4-training-mcp-skeleton.md](docs/superpowers/specs/2026-04-13-slice4-training-mcp-skeleton.md)
- Slice 6 spec: [docs/superpowers/specs/2026-04-15-slice6-durable-memory.md](docs/superpowers/specs/2026-04-15-slice6-durable-memory.md)
- Slice 6c plan: [docs/superpowers/plans/2026-04-17-slice6c-vault-scanner.md](docs/superpowers/plans/2026-04-17-slice6c-vault-scanner.md)
- Revised Slice 6c-6f spec: [docs/superpowers/specs/2026-04-18-slice6cdef-vault-scanner-memory-context-wiki-sync.md](docs/superpowers/specs/2026-04-18-slice6cdef-vault-scanner-memory-context-wiki-sync.md)
- Slice 8 spec: [docs/superpowers/specs/2026-04-15-slice8-proactive-autonomy.md](docs/superpowers/specs/2026-04-15-slice8-proactive-autonomy.md)
- Code quality plan: [docs/superpowers/plans/2026-04-15-code-quality-cleanup.md](docs/superpowers/plans/2026-04-15-code-quality-cleanup.md)
- Consolidation plan: [docs/superpowers/plans/2026-04-15-consolidation-and-refactor.md](docs/superpowers/plans/2026-04-15-consolidation-and-refactor.md)

## Verification Workflow

Run these before handoff, PR, or harness integration runs:

```bash
uv run pytest -q
uv run mypy minx_mcp
uv run ruff check .
```

Record the date and command outcome in the current handoff/PR notes, but do not freeze long-term health in this file with fixed counts.

### Interpreting mypy output

`uv run mypy` currently reports **0 errors in `minx_mcp/` (production source) and ~216 errors in `tests/`** (snapshot 2026-04-17 post-audit Waves 1–3). That total is expected, not a regression:

- Run `uv run mypy minx_mcp` by itself for the headline production signal — this must always return `Success: no issues found`.
- All test-side errors are the same loose-typing pattern: tests call MCP tools directly and index into the `ToolResponse` dict (typed `dict[str, Any]` by contract), so mypy cannot statically know that `result["data"]["memory"]["id"]` is an `int` on a specific call. Production callers either pass the envelope along unchanged or hydrate it into a typed dataclass, so they don't hit this at all.
- Dominant error codes in `tests/` are `[index]` (~~92), `[arg-type]` (~~59), `[unused-ignore]` (~~20), `[dict-item]` (~~12); none correspond to runtime bugs.
- When a PR or review asks "why is mypy noisy?", the answer is "test-only loose-dict indexing, see `docs/superpowers/plans/2026-04-15-code-quality-cleanup.md` §6.4". The test-only count is expected to trend down opportunistically as that phase lands, not in one sweep.
- The canonical template for tightening a single test file is what `tests/test_hermes_http_smoke.py::_call_tool` does: annotate the tool-call helper's return as `dict[str, Any]` (narrowing from `object`) so downstream indexing into `["data"][…]` is well-typed. Applying this pattern to one file dropped 9 errors from the baseline (205 → 196) on 2026-04-17.

The hard health rule is: `**minx_mcp/` source stays at 0 mypy errors, and every PR that touches a test file should not *increase* the `tests/` count**. Monotonic decrease is the goal; full zero is a nice-to-have, not a gate.

The post-hardening bump from 196 → 210 on 2026-04-17 (and the subsequent audit bump from 210 → 216) is a deliberate, documented exception: the hardening wave added 36+ and the post-audit waves added another ~28 regression tests (race interleavings, snapshot failure-policy, recipe savepoints, import-size limits, symlink escapes, log redaction, vault file locks, BaseService asyncio-task isolation, recipe reconcile, LLM error envelopes, DB-lock-during-LLM-await probes, `log_meal` atomicity) using the same loose-dict-indexing pattern the rest of the suite uses. The `ToolResponse[T]` generic / per-tool Pydantic DTO migration that would fix these at the source is explicitly deferred as a standalone refactor (see `docs/superpowers/plans/2026-04-15-code-quality-cleanup.md` §6.4) rather than being gold-plated into a hardening wave. The ratchet's intent — "don't casually grow test-side debt" — is preserved: we prefer this bump over sacrificing race-condition coverage.

## Post-Upgrade Operator Steps

After pulling the 2026-04-17 hardening wave (or any later revision that has not yet been deployed), run the following one-shot maintenance command before the next finance import:

```bash
python -m scripts.rebuild_finance_dedupe  # uses settings default DB path
# or: python -m scripts.rebuild_finance_dedupe /path/to/minx.db
```

This rebuilds `finance_transaction_dedupe` using the new merchant-aware fingerprint algorithm. Without it, re-importing the same source CSV/PDF after the upgrade would produce duplicate transactions because old fingerprints no longer match fresh hashes. The script is idempotent and savepoint-wrapped — running it more than once is safe and a no-op after the first successful run.

After pulling the 2026-04-18 Slice 6c migration, startup applies `018_vault_index.sql` automatically. Operators with production-like memory event history should run `PRAGMA integrity_check` after first startup because `018` rebuilds `memory_events` to re-add `vault_synced`. No manual scanner run is required, but Hermes can call `minx-core.vault_scan(dry_run=false)` when ready to index `Minx/` notes.

## Known Breaking Surface Changes (2026-04-17)

- **Finance report JSON payloads**: weekly/monthly report `summary` objects changed monetary fields from float dollars (e.g. `total_amount: -12.50`) to integer cents (`total_amount_cents: -1250`), and the `import_preview` `amount` field became a two-decimal string (`"-12.50"`) via `format_decimal_cents`. Any consumer outside the in-repo smoke tests that reads those fields by name must be updated in lockstep. Hermes integration was spot-checked via `tests/test_hermes_http_smoke.py`; no Hermes-side consumer reads those fields today, but the schema change is documented here so future consumers know to expect cents integers.
- **Meals MCP default `goal`**: omitting `goal` on the `nutrition_profile_set` tool used to implicitly mean `fat_loss`; it now means `maintenance` (no calorie multiplier applied). This is the safer default per the new `GOAL_CALORIE_MULTIPLIERS` semantics. Callers that want the old behavior must explicitly pass `goal="fat_loss"` (preserved as a legacy alias for `cut`).
- **Memory payload schemas**: `memory_create` and `memory_update_payload` now enforce per-`memory_type` payload shapes via pydantic models with `extra="forbid"` (see `minx_mcp/core/memory_payloads.py`). Known types are `preference`, `pattern`, `entity_fact`, `constraint`; unknown `memory_type` values still pass through unchanged for forward compatibility. Clients that previously stuffed arbitrary keys into `payload` will now receive `INVALID_INPUT` with the offending field named. `ingest_proposals` skips bad proposals with a WARNING log rather than aborting the batch.
- **LLM error contract**: upstream LLM failures (provider HTTP errors, malformed JSON, schema validation failures inside `interpretation/runner.py`) now surface with `error_code="LLM_ERROR"` instead of the generic `INTERNAL_ERROR`. Harnesses and clients that branch on error codes should add a case for `LLM_ERROR` (typically: retry once, then surface to the user as an upstream-model problem rather than a server bug).
- **`meals.recipe_orphaned` event**: new event emitted by `MealsService.reconcile_vault_recipes()` when a row's `vault_path` .md file is missing on disk; `vault_path` and `vault_synced_at` are nulled but the row itself (and any `meals_meal_entries.recipe_id` foreign keys) is preserved. Consumers of the event bus should add a handler or explicitly ignore `meals.recipe_orphaned`.
- **Rejected memory TTL**: `memory_reject` now sets `expires_at = now + 30 days`; `memory_list` / `get_pending_memory_candidates` call `prune_expired_memories()` before returning, so any tooling that relied on expired-rejected rows remaining visible indefinitely must instead query with `status="rejected"` within the 30-day window.
- **Vault memory notes**: Slice 6c requires `type: minx-memory` notes to carry `memory_key` in the format `{scope}.{memory_type}.{subject}` before they can sync into SQLite. `domain` is still accepted as a legacy alias for `scope`, but new notes/templates should use `scope`. Invalid memory notes are indexed with a warning and do not mutate `memories`.
