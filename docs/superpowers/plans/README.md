# Plans

Dated plans in this directory are implementation records. They may contain old paths, branch names, model choices, test counts, or unchecked tasks from the day they were written.

They should not be read as the current runbook. For current project state, use `../../../STATUS.md`; for operation, use `../../RUNBOOK.md`; for architecture, use `../../ARCHITECTURE.md`.
